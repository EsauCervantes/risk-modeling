# MLOps Reports

This folder is reserved for MLOps-specific artifacts such as monitoring outputs or run summaries.

The current wrapper scripts keep writing model metrics, validation predictions, and figures to the existing project-level `reports/` folders so the notebooks and command-line workflow use the same artifacts.
